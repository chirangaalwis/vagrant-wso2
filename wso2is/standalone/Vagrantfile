# -*- mode: ruby -*-
# vi: set ft=ruby :
Vagrant.configure("2") do |config|
  # Vagrant Box used
  config.vm.box = "ubuntu/trusty64"

  # Using 'forwarded_port's
  # config.vm.network "forwarded_port", guest: 9443, host: 9443, host_ip: "127.0.0.1", auto_correct: true
  # config.vm.network "forwarded_port", guest: 9763, host: 9763, host_ip: "127.0.0.1", auto_correct: true

  # Using a private network
  config.vm.network "private_network", ip: "192.168.0.3"

  # Share folder containing the JDK and the product pack
  config.vm.synced_folder "./files", "/home/vagrant"

  config.vm.provider "virtualbox" do |v|
    v.memory = 2500
    # v.cpus = 2
  end

  # Enable provisioning with a shell script.
  config.vm.provision "shell", inline: <<-SHELL
    apt-get update
    DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends --no-install-suggests curl iproute2 telnet unzip
    rm -rf /var/lib/apt/lists/*
    cd /home/vagrant
    if test ! -d java; then mkdir java; fi
    if test -d java; then
      tar -xf jdk-8u*-linux-x64.tar.gz -C /home/vagrant/java --strip-components=1
      export JAVA_HOME=/home/vagrant/java/

      if test -d java; then
        if test ! -d wso2is-5.4.0; then unzip wso2is-5.4.0.zip; fi

        if test -d wso2is-5.4.0; then
          chown -R vagrant:vagrant wso2is-5.4.0
          ./wso2is-5.4.0/bin/wso2server.sh
        fi
      fi
    fi
  SHELL
end
